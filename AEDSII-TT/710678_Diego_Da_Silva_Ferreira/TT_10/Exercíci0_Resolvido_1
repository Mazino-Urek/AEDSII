exercicio resolvido 1 - Seja nossa Pilha, faça um método que soma o 
conteúdo dos elementos contidos na mesma


public static void soma(){

	int resp = 0;

	for(Celula i = topo; i != null; i = i.proximo()){
		
		resp += i.elemento();
	}

	return resp;
}
