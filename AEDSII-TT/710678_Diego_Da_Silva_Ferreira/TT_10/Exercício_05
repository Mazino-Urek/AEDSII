exercicio 4 - Seja nossa Pilha, faça um método RECURSIVO para mostrar 
os elementos da pilha na ordem em que os mesmos foram inseridos


void mostrar(Celula i){

	if(i != NULL){
	
		mostrar(i.proximo());
		
		printf("%d", i.elemento());
	}
}

