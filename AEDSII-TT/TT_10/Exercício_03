exercicio 3- Seja nossa Pilha, faça um método RECURSIVO que retorna o 
maior elemento contido na pilha



int maior(int resp, Celula i){

	if(i != null){

		if(resp < i.elemento()){
			resp = i.elemento();
		}
		
		resp = maor(resp, i.proximo());
	}

	return resp;
}












