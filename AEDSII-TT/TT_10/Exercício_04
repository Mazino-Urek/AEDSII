exercicio 4 - Seja nossa Pilha, faça um método RECURSIVO para mostrar 
os elementos da pilha na ordem em que os mesmos serão removidos

void mostrar(Celula i){

	if(i != NULL){
	
		printf("%d", i.elemento());
		
		mostrar(i.proximo());
	}
}























